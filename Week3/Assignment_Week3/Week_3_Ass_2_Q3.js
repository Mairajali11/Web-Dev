// ? Question 3: Implement a simple logic that checks if both conditions are true using the logical AND
// ? operator (`&amp;&amp;`).

let condition1=false;
let condition2=true;

if (condition1 && condition2)
{
    console.log("Condition 1 and Conditon 2 are true.")
}
else{
    console.log("One or both conditions are false.")
}